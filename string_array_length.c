//To find out string array length
#include<stdio.h>
#include<string.h>

int main(){
	char ch[] = "3*2+(8+3)";
	int len = strlen(ch);
	printf("%d",len);
/*	for(int i=0;i<len;i++){
		printf("%c",ch[i]);
	}  */
	return 0;
}